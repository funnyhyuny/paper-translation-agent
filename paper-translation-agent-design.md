# 영문 논문 한국어 번역 아카이빙 에이전트 - 통합 설계서

> Claude Code 기반 구현을 위한 계획서. 본 문서는 구조와 역할 정의 수준까지만 다루며, 각 컴포넌트의 상세 구현은 실제 빌드 시점에 작성한다.

---

## 1. 작업 컨텍스트

### 1.1 배경 및 목적

영문 학술 논문을 개인 아카이브용 한국어 PDF로 변환한다. 기계 번역 서비스를 일회성으로 쓰는 것이 아니라, 다음 요건을 충족하는 **재현 가능한 파이프라인**을 구축하는 것이 목표다.

- 그림/표/수식 등 시각 자료가 유실되지 않을 것
- 용어 일관성이 유지된 자연스러운 한국어 번역일 것
- 깔끔한 조판으로 가독성이 확보될 것
- 사람이 중간 검토와 최종 수정을 개입할 수 있을 것

### 1.2 범위

**포함:**
- 단일 PDF 논문 1편을 한국어 PDF로 변환
- 본문, 초록, 그림, 표, 캡션, 각주, 참고문헌의 번역 및 재조판
- 수식은 보존(번역하지 않음), 주변 설명문만 번역
- 사람의 이미지 추출 검토 및 최종 번역 검토 단계

**제외:**
- 여러 논문 동시 처리 (배치 모드)
- PDF 외 입력 형식 (DOCX, LaTeX 소스 등)
- 학술 인용 스타일 변환

### 1.3 입출력 정의

**입력:**
- 작업 폴더 내 영문 논문 PDF 1개
- 사용자 지정 파일 경로

**출력:**
- `/output/<논문명>_KR.pdf` — 최종 한국어 번역본
- `/output/<논문명>_review/` — 중간 산출물 및 검토용 자료 (이미지, 마크다운, 용어집 등)

### 1.4 제약조건

| 항목 | 제약 |
|------|------|
| 실행 환경 | Claude Code (로컬, Python 실행 가능) |
| 레이아웃 전략 | 마크다운 재조판 방식 (원문 100% 일치 아님, 가독성 우선) |
| 번역 주체 | Claude Code 에이전트가 직접 수행 (별도 API 스크립트 X) |
| 병렬 처리 | 섹션 단위 서브에이전트 병렬 호출 |
| 수식 | 원본 LaTeX 그대로 보존, 번역 대상 아님 |
| 사람 검토 | 3회 (이미지 추출 후, 글로서리 추출 후, 최종 PDF 생성 후) |
| 대상 도메인 | ML/CS 컨퍼런스·저널·preprint, 수학/통계 이론, 응용·실험 논문(생물/의학/HCI) 등 광범위 학술 PDF |
| 입력 형식 한계 | 디지털 네이티브 PDF만 지원 (텍스트 추출 가능해야 함). 스캔본/이미지 PDF는 명시적으로 거부 |
| 사용 시나리오 | 개인 아카이브 + 가끔 동료·지인 공유. 표지/메타데이터/조판 품질이 단순 본인용보다 조금 더 엄격 |

### 1.5 용어 정의

| 용어 | 정의 |
|------|------|
| **메인 에이전트** | CLAUDE.md를 따르는 오케스트레이터. 전체 워크플로우 통제 |
| **서브에이전트** | 특정 섹션 번역을 담당하는 병렬 워커. `translator` |
| **스킬** | 결정론적 도구 모음. PDF 파싱, 이미지 추출, Typst 빌드 등 |
| **용어집(glossary)** | 본 번역 전에 추출한 핵심 용어의 영-한 매핑 사전 |
| **검토 게이트** | 사람의 확인을 기다리는 워크플로우 중단점 |
| **재조판** | 마크다운 구조를 Typst 템플릿에 부어 새 PDF를 생성하는 과정 |

---

## 2. 워크플로우 정의

### 2.1 전체 흐름

```
[입력 PDF 지정]
       │
       ▼
[1] PDF → 구조화 마크다운 변환  ──── 스크립트 (Marker)
       │
       ▼
[2] 그림/표 이미지 추출           ──── 스크립트 (Marker + PyMuPDF 썸네일)
       │
       ▼
┌─────────────────────────────┐
│ [검토 게이트 ①] 사람의 이미지 검토 │ ──── Human
└─────────────────────────────┘
       │
       ├─ 누락/오류 지적 → 재추출 (페이지/영역 지정)
       │
       ▼
[3] 용어집 추출                   ──── 에이전트 LLM 판단
       │
       ▼
┌─────────────────────────────┐
│ [검토 게이트 ③] 사람의 글로서리 검토 │ ──── Human
└─────────────────────────────┘
       │
       ├─ 잘못된 매핑 수정 → glossary.json 직접 편집 후 "OK"
       │
       ▼
[4] 섹션 분할                     ──── 스크립트
       │
       ▼
[5] 병렬 번역 (섹션 N개)          ──── 서브에이전트 N개 (translator)
       │                                각자 glossary.json 참조
       ▼
[6] 번역 결과 취합 + 자기 검증     ──── 에이전트 LLM 판단
       │
       ▼
[7] Typst 템플릿에 주입 → PDF 빌드 ──── 스크립트 (Typst)
       │
       ▼
┌─────────────────────────────┐
│ [검토 게이트 ②] 사람의 최종 검토   │ ──── Human
└─────────────────────────────┘
       │
       ├─ 수정 지시 (자유 텍스트) → 해당 부분만 재번역/재빌드
       │
       ▼
[완료] 최종 PDF 출력
```

### 2.2 단계별 상세

#### [1] PDF → 구조화 마크다운 변환

- **처리 주체**: 스크립트 (`pdf-parser` 스킬)
- **수행 내용**: Marker로 영문 PDF를 마크다운으로 변환. 수식은 LaTeX 형식 보존, 표는 마크다운 표로, 그림은 별도 파일로 추출 + 마크다운에 참조 삽입
- **성공 기준**: 마크다운 파일이 생성되고, 페이지 수 대비 텍스트 길이가 비정상적으로 짧지 않을 것 (페이지당 최소 200자 등)
- **검증 방법**: 규칙 기반 (텍스트 길이, 파일 생성 여부)
- **실패 시 처리**: 자동 재시도 1회 → 실패 시 사용자에게 에스컬레이션 (PDF가 스캔본일 가능성)

#### [2] 그림/표 이미지 추출

- **처리 주체**: 스크립트 (`pdf-parser` 스킬에 포함)
- **수행 내용**: Marker가 추출한 이미지를 `/output/<논문명>_review/images/`에 저장. PyMuPDF로 전체 페이지 썸네일 시트(`_thumbnail_sheet.pdf`) 생성하여 검토 편의성 확보
- **성공 기준**: 마크다운 내 모든 이미지 참조에 대응하는 파일이 존재할 것
- **검증 방법**: 규칙 기반 (참조-파일 매칭)
- **실패 시 처리**: 자동 재시도 1회 → 누락 시 사유 로그 후 게이트 ①로 진행 (사람이 판단)

#### [검토 게이트 ①] 이미지 검토

- **처리 주체**: Human
- **수행 내용**: 사용자가 `/output/<논문명>_review/images/`와 썸네일 시트를 보고 누락/오류 지적
- **성공 기준**: 사용자가 "진행", "OK", "다음" 등으로 승인하거나 구체적 수정 지시
- **검증 방법**: 사람 검토
- **재추출 입력 포맷**: 사용자는 **페이지 번호만 지정**한다 (예: "7페이지 그림 다시"). `recrop.py`는 해당 페이지 전체를 고해상도 PNG(예: 300 DPI)로 추출하여 `/images/page_NN_recrop.png`에 저장. 좌표 입력은 요구하지 않음 — 사용자 부담 최소화가 목적
- **실패 시 처리**: 재추출 후 다시 게이트 ①로 복귀 (사용자가 OK할 때까지 반복)

#### [3] 용어집 추출

- **처리 주체**: 메인 에이전트 (LLM 판단)
- **수행 내용**: 마크다운 전체를 읽고 핵심 용어를 추출하여 영-한 매핑 생성. 대상: 고유명사, 기술용어, 저자가 정의한 개념(약어는 원문 유지 정책이므로 별도 매핑 불필요하나 약어→풀이는 참고용으로 포함 가능). `glossary.json` 저장
- **표기 정책**: `ko` 필드는 **"원어 유지 + 첫 등장 한국어 병기"** 방식을 가정하여 작성. 예: `{en: "attention mechanism", ko: "어텐션 메커니즘", note: "첫 등장 시 'attention mechanism(어텐션 메커니즘)', 이후 원어"}`. 약어(CNN, LSTM 등)는 원문 유지하며 한국어 병기 안 함을 default로 함
- **비용 사전 안내**: 마크다운 분량과 섹션 수를 기반으로 **번역 단계의 예상 토큰 사용량과 비용**을 게이트 ③ 시점에 사용자에게 안내. 사용자가 비용을 보고 진행 여부 판단
- **성공 기준**: 최소 5개 이상 용어가 추출되고, 각 항목이 `{en, ko, note}` 스키마를 따를 것
- **검증 방법**: 스키마 검증 (필수 필드, 항목 수)
- **실패 시 처리**: 자동 재시도 1회 → 사용자에게 스킵 확인 (용어집 없이 진행 가능, 일관성 떨어질 위험만 고지)

#### [검토 게이트 ③] 글로서리 검토

- **처리 주체**: Human
- **수행 내용**: 사용자는 `/output/<논문명>_review/glossary.json`을 텍스트 에디터로 직접 열어 잘못된 매핑을 수정하거나 항목을 추가/삭제. 메인 에이전트는 사용자에게 파일 경로와 함께 "파일을 열어 ko 필드를 수정하고 저장 후 OK라고 알려주세요"라는 안내를 출력
- **추가 안내**: 같은 메시지에 예상 토큰량 + 비용을 함께 표시
- **성공 기준**: 사용자가 "OK" 또는 "진행"으로 승인. 파일 변경이 있으면 메인 에이전트가 변경된 스키마 재검증
- **검증 방법**: 사람 검토 + JSON 스키마 재검증
- **실패 시 처리**: JSON 파싱 실패 시 사용자에게 오류 위치 안내 후 재편집 요청
- **존재 이유**: 글로서리는 N개 섹션 전체의 용어를 결정짓는 단일 지점. 잘못된 매핑이 들어가면 N개 섹션 모두 동일하게 오염되고, 게이트 ②에서 발견하면 수정 비용이 큼. 1회 추가되는 사람 대기 시간 < 사후 수정 비용

#### [4] 섹션 분할

- **처리 주체**: 스크립트 (`section-splitter` 스킬)
- **수행 내용**: 마크다운의 `#`, `##` 헤더 기준으로 섹션 분리. 각 섹션을 `/output/<논문명>_review/sections/sec_N.md`로 저장
- **이차 분할(장문 섹션 처리)**: 어떤 섹션의 분량이 임계값을 넘으면(예: 6000단어 이상) H3 헤더 기준으로 한 번 더 쪼개 `sec_N_a.md`, `sec_N_b.md` 등으로 분리. 학술 논문은 Introduction이나 Methods가 H3까지 내려가는 경우가 많으므로 자연스러운 경계가 존재함. 컨텍스트 압축으로 인한 품질 저하 방지가 목적
- **References 처리**: References 섹션은 분할 시 별도로 식별하고 **번역 대상에서 제외 플래그**를 단다(메타데이터로 `{translate: false}`). 번역 단계는 이 섹션을 스킵하고, 빌드 단계에서 원문을 그대로 첨부
- **성공 기준**: 최소 2개 이상 섹션이 생성될 것
- **검증 방법**: 규칙 기반 (파일 개수)
- **실패 시 처리**: 헤더가 부족하면 전체를 단일 섹션으로 처리하고 병렬화 스킵 (로그 기록)

#### [5] 병렬 번역

- **처리 주체**: 서브에이전트 `translator` N개 병렬 호출
- **수행 내용**: 각 워커가 한 섹션을 담당. 입력: `sec_N.md` 경로 + `glossary.json` 경로 + `style_guide` (AGENT.md에 임베드) + 섹션 인덱스. 작업: 영→한 번역, 수식/이미지 참조/표 구조 보존, 용어집 준수. 출력: `sec_N_kr.md`
- **새 용어 발견 시**: glossary에 없는 전문용어를 마주치면 워커가 자체 판단으로 번역하되 `/output/<논문명>_review/sections_kr/sec_N_addendum.json`에 `{en, ko, note: "translator 자체 추가"}` 형식으로 기록. 충돌 해결은 [6]에서 메인이 일괄 처리
- **완료 마커**: 워커는 작업 완료 시 `sec_N_kr.md`의 마지막 줄에 `<!-- DONE -->` 마커를 기록한다. 부분 작업/타임아웃 등으로 파일이 존재하지만 작업이 미완료된 상황을 식별하기 위함
- **성공 기준**: 모든 섹션의 한국어 파일이 생성되고, 각 파일이 `<!-- DONE -->` 마커로 끝나며, 원문의 이미지 참조(`![](...)`)와 수식 블록(`$$...$$`) 개수가 그대로 보존될 것
- **검증 방법** (translation-validator 스킬):
  1. `<!-- DONE -->` 마커 존재 확인
  2. 이미지/수식 개수 매칭 (원문 = 번역)
  3. 길이 비율 체크: `len(한국어) / len(영문)`이 0.6–1.2 범위 (벗어나면 부분 누락 또는 환각 의심)
  4. 마크다운 문법 유효성 (괄호/표/링크)
- **실패 시 처리**: 검증 실패한 섹션만 자동 재시도 1회 → 그래도 실패 시 사용자에게 보고

#### [6] 번역 결과 취합 + 자기 검증

- **처리 주체**: 메인 에이전트 (LLM 판단)
- **수행 내용**:
  1. **Addendum 머지**: 모든 `sec_N_addendum.json`을 수집. 같은 영어 용어에 대해 서로 다른 한국어 매핑이 있으면(예: A워커=미세조정, B워커=파인튜닝) **메인이 단독으로 하나를 선택** → 관련 섹션에 grep+치환으로 일괄 적용. 사용자에게 묻지 않고 진행하되 progress.log에 결정 기록(추후 게이트 ②에서 사용자가 확인 가능)
  2. **통합 마크다운 생성**: `sec_N_kr.md`들을 원래 순서로 합쳐 `merged_kr.md` 생성
- **자기 검증 체크리스트**:
  1. **Glossary 준수**: 마크다운 전체를 grep해 glossary 외 표기로 번역된 용어 탐지
  2. **섹션 경계 어색함**: 각 섹션 경계 ±300자를 읽고 어조/문맥 끊김 LLM 판단
  3. **이미지/수식 개수 매칭**: translation-validator 스크립트 결과 재확인
  4. (References 등 `translate: false` 섹션은 검증 제외)
- **성공 기준**: 위 체크리스트 모두 통과
- **실패 시 처리**: 문제 섹션만 재번역 요청. 글로서리 위반은 grep+치환으로 즉시 수정

#### [7] Typst 템플릿 빌드

- **처리 주체**: 스크립트 (`typst-builder` 스킬)
- **수행 내용**: 마크다운을 Typst 소스로 변환 → 템플릿에 주입 → `typst compile`로 PDF 생성. 마지막에 References 원문을 그대로 첨부
- **템플릿 디자인 결정사항**:
  - **단 구성**: 1단 고정 (한국어는 글자 폭이 넓어 2단에서 괄호/병기 처리가 까다로움)
  - **그림/표 배치**: 그림과 표는 **본문에서 처음 참조되는 지점 직뒤에 인라인 삽입**. Typst `figure()`의 floating placement는 사용하지 않음 (그림-설명 거리가 멀어지지 않도록)
  - **그림 너비**: 그림은 전폭(full-width), 표도 본문 폭 안에 들어가도록 (필요 시 폰트 축소 또는 가로 회전)
  - **첨부 요소**: ① 표지 페이지 (한국어+원문 제목, 저자, 원문 출시정보, 번역일), ② 자동 생성 목차, ③ 페이지 머리말/꼬리말(논문명 + 페이지번호). 번역 주(footnote)는 도입하지 않음
  - **폰트**: 본문/제목 모두 **Noto Sans KR** (모던 산세리프 통일). 수식은 Typst 기본 수학 폰트. 영문 코드/식별자는 모노스페이스 폴백
  - **References**: 별도 섹션으로 원문 그대로 첨부 (제목만 "참고문헌"으로 번역)
- **성공 기준**: PDF 파일이 정상 생성되고, 페이지 수가 원문과 ±30% 이내일 것
- **검증 방법**: 규칙 기반 (파일 존재, 페이지 수 범위)
- **실패 시 처리**: 자동 재시도 1회 (Typst 컴파일 에러 로그 분석 후 마크다운 정리) → 실패 시 에스컬레이션

#### [검토 게이트 ②] 최종 검토

- **처리 주체**: Human
- **수행 내용**: 사용자가 최종 PDF 검토 후 **자유 텍스트로 수정 지시** (예: "12페이지 첫 문단 어색함", "Figure 3 캡션 다시", "backbone을 백본으로 통일"). 별도 포맷 강제하지 않음
- **재번역 단위**: 메인 에이전트가 지시 종류에 따라 판단
  - "문장 어색함" / "오타" → 문단 단위 부분 수정
  - "Figure N 캡션" / "수식 X 옆 설명" → 해당 블록만 수정
  - "용어 X를 Y로 변경" → 전체 grep+치환 (glossary.json도 함께 업데이트)
  - "Section N 톤이 다름" → 해당 섹션 통째 재번역 (translator 재호출)
- **성공 기준**: 사용자가 "완료" 또는 명시적 승인
- **검증 방법**: 사람 검토
- **실패 시 처리**: 수정 → [7] 재실행 → 게이트 ②로 복귀 (사용자가 완료할 때까지 반복)

### 2.3 LLM 판단 영역 vs 코드 처리 영역

| 단계 | LLM 판단 | 스크립트 처리 |
|------|---------|---------------|
| [1] PDF 변환 | — | ✅ Marker 호출 |
| [2] 이미지 추출 | — | ✅ Marker + PyMuPDF |
| [3] 용어집 추출 | ✅ 의미 기반 용어 선별 | — |
| [4] 섹션 분할 | — | ✅ 헤더 파싱 |
| [5] 번역 | ✅ 핵심 영역 (자연어 생성) | — |
| [6] 취합/검증 | ✅ 정성적 품질 판단 | 파일 결합은 스크립트 |
| [7] Typst 빌드 | — | ✅ 템플릿 주입 + 컴파일 |
| 게이트 ①② | (사람) | — |
| 수정 지시 해석 | ✅ 자유 텍스트 → 작업 항목 변환 | — |

---

## 3. 구현 스펙

### 3.1 폴더 구조

```
/paper-translator
  ├── CLAUDE.md                              # 메인 에이전트 지침 (오케스트레이터)
  ├── /.claude
  │   ├── /skills
  │   │   ├── /pdf-parser
  │   │   │   ├── SKILL.md
  │   │   │   ├── /scripts
  │   │   │   │   ├── parse_pdf.py           # Marker 호출, MD + 이미지 추출
  │   │   │   │   ├── make_thumbnail.py      # PyMuPDF로 페이지 썸네일 시트 생성
  │   │   │   │   └── recrop.py              # 사용자 지정 영역 재크롭
  │   │   │   └── /references
  │   │   │       └── marker_usage.md        # Marker 옵션 가이드
  │   │   ├── /section-splitter
  │   │   │   ├── SKILL.md
  │   │   │   └── /scripts
  │   │   │       └── split.py               # 헤더 기준 마크다운 분할
  │   │   ├── /translation-validator
  │   │   │   ├── SKILL.md
  │   │   │   └── /scripts
  │   │   │       └── validate.py            # 이미지/수식 개수 매칭 등 규칙 검증
  │   │   └── /typst-builder
  │   │       ├── SKILL.md
  │   │       ├── /scripts
  │   │       │   ├── md_to_typst.py         # 마크다운 → Typst 변환
  │   │       │   └── build_pdf.py           # typst compile 호출
  │   │       └── /templates
  │   │           └── paper.typ              # 논문용 Typst 템플릿 (한국어 폰트 적용)
  │   └── /agents
  │       └── /translator
  │           └── AGENT.md                   # 섹션 번역 서브에이전트 지침
  ├── /input                                 # 원본 영문 PDF 위치
  ├── /output
  │   └── <논문명>_review/                    # 중간 산출물
  │       ├── source.md                      # [1] 단계 결과
  │       ├── images/                        # [2] 단계 추출 이미지
  │       │   └── _thumbnail_sheet.pdf       # 검토용 썸네일
  │       ├── glossary.json                  # [3] 단계 용어집
  │       ├── sections/                      # [4] 단계 섹션 분할
  │       │   ├── sec_0.md
  │       │   └── sec_N.md
  │       ├── sections_kr/                   # [5] 단계 번역 결과
  │       │   └── sec_N_kr.md
  │       └── merged_kr.md                   # [6] 단계 통합 마크다운
  │   └── <논문명>_KR.pdf                     # [7] 단계 최종 산출물
  └── /docs                                  # (선택) 참고 문서
```

### 3.2 CLAUDE.md 핵심 섹션 목록

> 실제 내용은 구현 시 작성. 아래는 포함되어야 할 섹션 목록만.

1. **역할 정의**: 오케스트레이터로서 7단계 워크플로우를 순차 진행
2. **워크플로우 진행 규칙**: 각 단계의 진입 조건, 성공 조건, 다음 단계로의 전이
3. **검토 게이트 운영**: 사용자 입력 대기 방법, 자유 텍스트 해석 가이드
4. **서브에이전트 호출 규칙**: `translator` 병렬 호출 시점, 입출력 명세 전달 방식
5. **스킬 호출 가이드**: 각 스킬을 언제 호출할지의 트리거 조건
6. **실패 처리 정책**: 재시도 횟수, 에스컬레이션 메시지 포맷
7. **상태 관리**: 현재 어느 단계에 있는지 추적 (간단한 진행 로그 파일)

### 3.3 에이전트 구조

**메인 에이전트 1개 + 서브에이전트 1종**

- **메인 (CLAUDE.md)**: 오케스트레이터. 워크플로우 전 단계를 진행하며 스킬과 서브에이전트를 호출
- **서브에이전트 `translator`**: 번역 전용. 컨텍스트 윈도우 최적화 목적 — 번역 지침(톤, 용어집 활용법, 수식 보존 규칙 등)은 분량이 많고 다른 단계에서는 불필요하므로 분리. 또한 병렬 호출이 본질적으로 필요한 단계이므로 서브에이전트가 자연스러운 단위.

> **단일 에이전트로 가지 않는 이유**: 번역 단계만 병렬 처리가 필요하고, 번역에 특화된 긴 지침(스타일 가이드, 학술 어조, 수식 처리 규칙)이 메인 컨텍스트에 항상 있을 필요가 없다.

### 3.4 단계별 처리 방식

| 단계 | 처리 방식 | 호출 대상 |
|------|----------|----------|
| [1] PDF 변환 | 메인이 스킬 호출 | `pdf-parser` |
| [2] 이미지 추출 | 메인이 스킬 호출 | `pdf-parser` |
| 게이트 ① | 메인이 사용자 메시지 대기 | — |
| [3] 용어집 추출 | 메인 에이전트 직접 LLM 작업 | — |
| [4] 섹션 분할 | 메인이 스킬 호출 | `section-splitter` |
| [5] 번역 | 메인이 서브에이전트 N개 병렬 호출 | `translator` × N |
| [6] 취합/검증 | 메인 에이전트 직접 LLM 작업 + 스킬 호출 (규칙 검증) | `translation-validator` |
| [7] Typst 빌드 | 메인이 스킬 호출 | `typst-builder` |
| 게이트 ② | 메인이 사용자 메시지 대기 | — |
| 수정 처리 | 메인 에이전트가 지시 해석 → 부분 재실행 | (해당 단계 재호출) |

### 3.5 스킬 명세

| 스킬명 | 역할 | 트리거 조건 |
|--------|------|------------|
| **pdf-parser** | PDF → 마크다운 + 이미지 추출, 썸네일 시트 생성, 재크롭 | 워크플로우 [1]~[2] 진입 시, 게이트 ①에서 재추출 요청 시 |
| **section-splitter** | 마크다운을 헤더 기준 섹션 파일들로 분할 | 워크플로우 [4] 진입 시 |
| **translation-validator** | 번역 결과의 이미지/수식 개수 매칭, 마크다운 문법 검증 | 워크플로우 [5] 완료 후, [6] 자기 검증 단계에서 |
| **typst-builder** | 마크다운 → Typst → PDF 컴파일 (한국어 폰트 적용) | 워크플로우 [7] 진입 시, 게이트 ② 수정 후 재빌드 시 |

### 3.6 서브에이전트 명세

**`translator`**

- **역할**: 단일 마크다운 섹션을 한국어로 번역
- **트리거 조건**: 메인 에이전트가 워크플로우 [5] 단계에서 섹션별로 병렬 호출
- **입력**:
  - 섹션 마크다운 파일 경로 (`/output/<논문명>_review/sections/sec_N.md`)
  - 용어집 파일 경로 (`/output/<논문명>_review/glossary.json`)
  - 섹션 인덱스 N
- **출력**:
  - 한국어 번역 마크다운 파일 (`/output/<논문명>_review/sections_kr/sec_N_kr.md`) — 마지막 줄에 `<!-- DONE -->` 마커
  - (선택) `sec_N_addendum.json` — glossary에 없는 새 용어 매핑 기록
- **참조 스킬**: 없음 (순수 LLM 작업)
- **AGENT.md 핵심 행동 규칙**:

  **(A) 톤과 종결 어미**
  - 평어체 사용 (경어체 금지)
  - **의미에 따라 종결 어미 혼용**: Methods·Results처럼 수행한 내용 서술은 과거시제(`–하였다`, `–이었다`), 일반 진술·이론 서술은 현재시제(`–한다`, `–이다`)
  - 학술 논문 톤 유지 (구어체 표현 금지)

  **(B) 용어 표기**
  - Glossary의 매핑 엄격 준수
  - **원어 유지 + 첫 등장 한국어 병기** 원칙: `attention mechanism(어텐션 메커니즘)`으로 첫 등장, 이후 `attention mechanism`
  - **약어는 원문 그대로** (CNN, LSTM, BERT — 한국어 병기 없음)
  - Glossary에 없는 새 전문용어 발견 시: 자체 판단으로 번역하되 `sec_N_addendum.json`에 기록

  **(C) 보존 규칙**
  - 수식 블록(`$...$`, `$$...$$`) 내부는 절대 수정하지 않음 (`\text{...}` 안의 영문도 그대로)
  - 이미지 참조(`![alt](path)`)는 경로 유지, alt 텍스트만 번역
  - 표 구조(파이프) 유지, 셀 내용만 번역
  - **Citation**: `[Smith et al., 2020]`, `[12]` 등은 원문 유지
  - **Cross-reference**: `Figure 2` → `그림 2`, `Table 1` → `표 1`, `Section 3` → `3절`, `Eq. (3)` → `식 (3)` (Typst 빌드 단계에서 자동 매핑되도록 마크다운에서도 한국어 레이블 사용)
  - References 섹션 자체가 입력으로 오면 작업하지 않고 `<!-- SKIPPED: references -->` 마커만 출력 (안전장치 — 정상 흐름에선 splitter가 미리 제외)

  **(D) 완료 마커**
  - 작업이 끝까지 완료되면 출력 파일의 마지막 줄에 정확히 `<!-- DONE -->` 추가

### 3.7 데이터 전달 패턴

| 데이터 | 전달 방식 | 위치 |
|--------|----------|------|
| 마크다운 파일들 | 파일 기반 | `/output/<논문명>_review/` 하위 |
| 용어집 | 파일 기반 (JSON) | `/output/<논문명>_review/glossary.json` |
| 서브에이전트 입력 | 프롬프트 인라인 (파일 경로만 전달) | — |
| 사용자 검토 신호 | 프롬프트 인라인 (자유 텍스트) | — |
| 검증 결과 | 프롬프트 인라인 (스크립트가 stdout 반환) | — |

원칙: **모든 중간 산출물은 디스크에 저장**, 에이전트 간에는 경로만 주고받는다. 컨텍스트 윈도우 절약 + 디버깅 용이성 확보.

### 3.8 주요 산출물 파일 형식

| 파일 | 형식 | 비고 |
|------|------|------|
| `source.md` | Markdown | Marker 출력. 이미지 참조, LaTeX 수식 포함 |
| `images/*.png` | PNG | Marker가 추출한 그림/표 이미지 |
| `images/page_NN_recrop.png` | PNG | 사용자 요청 시 게이트 ①에서 페이지 단위 고해상도 재추출 |
| `_thumbnail_sheet.pdf` | PDF | 페이지 썸네일을 격자로 모은 검토용 시트 |
| `glossary.json` | JSON | `[{en, ko, note}]` 배열. 게이트 ③에서 사용자가 직접 편집 |
| `sec_N.md`, `sec_N_kr.md` | Markdown | 섹션 단위 영문/번역문. 한국어판은 `<!-- DONE -->` 마커로 종결 |
| `sec_N_addendum.json` | JSON | 워커가 glossary 외 신규 용어 기록 |
| `merged_kr.md` | Markdown | 번역 통합본 |
| `paper.typ` | Typst | 조판 템플릿 (Noto Sans KR 설정 포함) |
| `<논문명>_KR.pdf` | PDF | 최종 산출물 |
| `progress.log` | Text | 단계 진행 상태 기록 (간단한 키-값). 재개 시 메인이 읽어 완료 단계 스킵 |

### 3.9 상태 관리 / 재개 정책

- **`progress.log` 포맷**: 각 단계의 시작/완료 시각, 상태(`Done`/`Failed`/`InProgress`), 마지막 산출물 경로
- **재개 동작**: 메인 에이전트가 시작 시 progress.log를 읽고, `Done`으로 마크된 단계는 자동 스킵 → 마지막 실패/미완료 지점부터 재시작. 중단된 워커 작업은 `<!-- DONE -->` 마커 부재로 식별하여 재시도
- **다중 논문 처리**: `/input/` 안의 여러 PDF는 각각 `/output/<논문명>_review/`로 자동 격리. 진행 중 작업이 있으면 별도 진행 상태 유지

### 3.10 환경 / 설치 의존성 (구현 시 우려 사항)

> 본 시스템의 가장 큰 운영상 마찰점은 **외부 도구 설치 복잡도**다. 다음 사항을 README/setup 스크립트로 정리해야 한다.

| 의존성 | 설치 난이도 | 비고 |
|--------|------------|------|
| **Marker** | 중 | Python 패키지 + 모델 다운로드 (수 GB). GPU 사용 시 CUDA 설정. CPU 모드는 느림 |
| **Typst** | 하 | 바이너리 단일 설치. Homebrew/직접 다운로드 |
| **Noto Sans KR** | 하 | Google Fonts에서 무료. Typst가 시스템 폰트 또는 프로젝트 폴더에서 인식 |
| **PyMuPDF** | 하 | `pip install pymupdf` |

**완화책**:
- `setup.sh` 스크립트로 일괄 설치 (Marker 모델 캐시 검증 포함)
- Marker가 최초 실행 시 모델을 다운받으므로 첫 논문은 시간이 더 걸린다는 안내
- Typst 컴파일 시 폰트 미발견 에러 메시지를 캐치하여 사용자에게 폰트 설치 안내 출력

---

## 4. 설계 결정 요약

| 결정 | 선택 | 근거 |
|------|------|------|
| 레이아웃 방식 | 마크다운 재조판 (B) | 사용자 선택 |
| PDF 파싱 | Marker (디지털 PDF만) | 학술 논문 특화. 스캔본은 명시적 거부 |
| 조판 도구 | Typst | LaTeX 대비 한국어 폰트 설정 단순, 컴파일 속도 빠름 |
| 한국어 폰트 | Noto Sans KR (본문/제목 통일) | 모던 산세리프, 화면 독서 가독성 |
| 단 구성 | 1단 + 그림/표 독립 배치 | 한국어 글자 폭이 넓어 2단에 부적합. 가독성 최우선 |
| 그림 배치 | 처음 참조되는 지점 직뒤 인라인 | 그림-설명 거리 최소화 |
| 첨부 요소 | 표지 + 목차 + 머리말/꼬리말 | 동료 공유 가능 수준의 아카이브 품질 |
| 병렬 단위 | 섹션 (H2). 장문은 H3 이차 분할 | 자연스러운 경계, 컨텍스트 압축 방지 |
| 용어 일관성 | 사전 추출 글로서리 + 게이트 ③ + 사후 자기검증 | 병렬 번역의 핵심 일관성 보장 장치 |
| 글로서리 편집 UX | JSON 직접 편집 안내, "OK" 입력 | 가장 직접적, 제어권 최대 |
| 문체 일관성 | AGENT.md 스타일 가이드로 강제 | 병렬 워커 간 어조 통일 |
| 종결 어미 | Methods/Results 과거, 이론/일반 현재 | 영문 원문의 시제 의도 반영 |
| 전문용어 표기 | 원어 유지 + 첫 등장 한국어 병기 | ML/CS 학계 관행, 검색성 |
| 약어 | 원문 그대로, 한국어 병기 없음 | 약어 자체가 식별자, 중복 표기 회피 |
| Citation | `[Smith et al., 2020]`, `[12]` 원문 유지 | 출처 추적성 |
| Figure/Table 레이블 | `Figure 2` → `그림 2`로 한국어화 | 본문 자연스러움, 캡션과 통일 |
| 수식 처리 | LaTeX 보존, `\text{}` 안 영문도 유지 | 수식 연속성, 렌더링 안정성 |
| References | 번역 제외, 원문 그대로 첨부 | 출처 추적, 분량/조판 모두 절약 |
| 사람 검토 횟수 | 3회 (이미지 / 글로서리 / 최종) | 글로서리 게이트로 사후 수정 비용 회피 |
| 사람 검토 입력 | 자유 텍스트, 메인이 해석 | 단순성 우선 |
| 재번역 단위 | 지시 종류에 따라 메인이 판단 | 문맥 보존과 정확성 균형 |
| 이미지 재추출 입력 | 페이지 번호만, 페이지 전체 PNG | 사용자 부담 최소화 |
| 비용 관리 | 게이트 ③에서 예상치 안내, 수동 승인 | 토큰 예산 사용자 통제 |
| 재개 정책 | progress.log + `<!-- DONE -->` 마커 | 중단된 작업 식별 가능 |
| 다중 논문 | 논문당 하위 폴더 자동 생성 | 상태 충돌 회피 |
| Addendum 충돌 | 메인이 단독 판단 후 일괄 적용 | 사용자 질문 최소화 |
| 새 용어 탐지 | translator 자체 판단 + addendum 기록 | 병렬성 유지하며 추적성 확보 |
| 실패 감지 | `<!-- DONE -->` + 길이비 + 개수매칭 | 다층 검증 |
| 에이전트 구조 | 메인 1 + 서브 1 (translator) | 번역 단계의 컨텍스트 분리와 병렬화 필요성 |

---

## 5. 구현 시 추가 결정이 필요한 항목

(설계서 범위 외, 실제 구현 단계에서 결정)

- Marker GPU/CPU 모드 선택 (성능 vs 환경 단순성)
- `progress.log` 포맷의 구체화 (텍스트 키-값 vs JSON)
- 재시도 정책의 구체 횟수 (현 설계서는 "1회"로 일률화)
- 용어집 추출 시 사용할 프롬프트 패턴
- 장문 섹션 임계값의 구체 수치(현재 6000단어 가정)
- AGENT.md 스타일 가이드의 구체 문장 예시 모음 (good/bad 사례)
- Typst 템플릿의 미세 조판 (행간, 자간, 들여쓰기, 표지 디자인)
- 비용 예상치 산출 공식 (입력 토큰 × 단가 + 출력 토큰 × 단가, 안전 마진)

---

## 6. 잔존 리스크 / 알려진 한계

| 리스크 | 영향 | 완화 |
|--------|------|------|
| **Marker 표/수식 파싱 실패** | 본문 구조 손상 | 폴백 없음. 게이트 ①·②에서 사용자가 발견하면 해당 페이지 재추출 또는 수동 수정 |
| **외부 도구 설치 부담** | 첫 사용 시 진입 장벽 | `setup.sh` + 명확한 에러 메시지 |
| **장문 논문 토큰 비용** | 1편당 수십만~수백만 원 가능 | 게이트 ③ 사전 예상치 안내 + 수동 승인 |
| **글로서리 추출 누락** | 일관성 저하 | translator의 addendum 기록 + [6] 자기검증 |
| **번역 환각** | 잘못된 정보 전달 | 길이비 체크 + 사용자 최종 검토 (게이트 ②) |
| **References 첨부 시 폰트 호환** | 원문에 특수 문자/언어 포함 시 깨질 가능 | Noto Sans KR의 폴백 폰트 체인 설정 |
| **메인 에이전트가 임의 종료 판단** | 워크플로우 중도 중단 | CLAUDE.md에 "각 단계를 명시적으로 완료시키고 게이트 외에는 사용자 개입 없이 진행"을 강제 명시 |
